@page
@model Crud.Pages.GroupCrud.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h3>Details</h3>

<div>
    <br />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Group.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Group.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Group?.Id"class="btn btn-success">Edit</a> 
    <a asp-page="./Index"class="btn btn-secondary">Cancel</a>
</div>
